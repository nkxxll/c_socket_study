#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/socket.h>
#include <sys/un.h>

int main(int argc, char *argv[]) {
  // make socket
  // bind
  // listen
  // accept
  // echo
  return EXIT_SUCCESS;
}
